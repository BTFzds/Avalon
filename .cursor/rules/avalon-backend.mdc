---
description: Avalon 后端（FastAPI/WebSocket）约定
globs: "**/*.py"
alwaysApply: false
---

# 后端约定

- FastAPI：路由/房间管理/规则引擎分模块；Pydantic 校验入站消息。
- `async def` 用于 WS 与 IO；类型注解完整；配置用环境变量。
- 房间是权威状态容器：阶段、领袖、任务进度、投票箱、角色映射。
- 广播公开事件 + 定向私密事件；绝不把全员角色表发给普通客户端。
- 错误返回明确 reason（阶段不符、未人齐、非法组队人数等），便于前端提示。
- 先保证正确性与可测规则，再谈规模化（多进程、Redis 等非第一版必需）。
