---
description: Avalon 技术栈、状态机与实时架构约束
alwaysApply: true
---

# Avalon 架构

## 技术选型（已定）
- 前端：React + TypeScript + Vite + Tailwind（移动优先布局）。
- 后端：Python FastAPI + WebSocket 房间实时通道。
- 第一版房间状态可内存为主；持久化非优先。

## 领域优先
- 把「规则引擎 / 房间状态机」与 UI、传输层分离；配置表（人数→任务人数/角色）单一数据源。
- 用明确阶段建模（如 lobby → night → team_propose → team_vote → quest → assassin → ended），非法迁移直接拒绝。
- 复杂局内状态优先清晰状态机或 reducer；避免把规则散落在组件事件里。

## 实时与安全
- WebSocket 消息契约类型化（共享或镜像类型）；校验发送者身份与当前阶段。
- 私密视野（梅林/坏人/派西等）只推给有权限的连接；公开视图对全员一致。
- 同时投票/出牌：服务端收齐或超时策略后再结算；勿在客户端提前泄露计数细节。

## 实现原则
- 可读性优先；命名常量替代魔法数（任务人数、否决上限等）。
- 小步改动、模块化、DRY；不引入未要求的功能或依赖。
- 关键规则与状态迁移应可单测；边缘情况（满否决、双失败第 4 轮、刺杀）必须覆盖。
